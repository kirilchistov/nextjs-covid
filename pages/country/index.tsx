import { useRef } from 'react';
import Header from '../../components/Header';

const country = () => {
  return (
    <div>
      <Header />
      Country page
    </div>
  );
};

export default country;
